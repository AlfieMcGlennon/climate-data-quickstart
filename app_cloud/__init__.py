"""Education-first cloud-hosted variant of the climate-data quickstart app.

Entry point: ``streamlit run app_cloud/main.py``.

Imports from ``app/`` are read-only. The desktop app at ``app/main.py``
remains the canonical local experience and is never edited from here.
"""
